# docker-compose.yml for Synology NAS (Container Manager)
# Customize the absolute paths (/volume1/docker/...) to match your actual Synology folder structure.

services:
  argusflix_manager:
    # Option 1 (Recommended): Use the official pre-built image and mount your custom plugins/scripts.
    image: ghcr.io/argusflix_manager/argusflix_manager:latest
    
    # Option 2 (Uncomment if you made changes to the core code of ArgusFlix-0.25.1):
    # build:
    #   context: /volume1/docker/argusflix_manager/ArgusFlix-0.25.1
    #   dockerfile: docker/Dockerfile
    
    container_name: argusflix_manager
    restart: unless-stopped
    ports:
      - 9191:9191
    volumes:
      # Data persistence (database, logs, uploads, backups)
      - /volume1/docker/argusflix_manager/data:/data
      
      # Custom plugins directory
      - /volume1/docker/argusflix_manager/plugins:/data/plugins
      
      # Custom scripts directory
      - /volume1/docker/argusflix_manager/scripts:/data/scripts
      
    environment:
      - ARGUSFLIX_ENV=aio
      - REDIS_HOST=localhost
      - CELERY_BROKER_URL=redis://localhost:6379/0
      - ARGUSFLIX_LOG_LEVEL=info
      - ARGUSFLIX_ALLOWED_SCRIPT_DIRS=/data/scripts
      
      # Optional: Synology specific environment variables (e.g., timezone, PUID/PGID if supported)
      - TZ=Europe/Berlin
      
    # Optional: Hardware acceleration (Intel QuickSync / VA-API)
    # Synology models with Intel CPUs support hardware transcoding.
    #devices:
    #  - /dev/dri:/dev/dri
